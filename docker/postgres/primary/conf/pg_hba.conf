# PostgreSQL Client Authentication Configuration File
# ===================================================

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust

# IPv4 local connections
host    all             all             127.0.0.1/32            trust

# IPv6 local connections
host    all             all             ::1/128                 trust

# Docker network connections
host    all             trainadmin      0.0.0.0/0               md5
host    replication     replicator      0.0.0.0/0               md5
host    all             replicator      0.0.0.0/0               md5

# Allow replication connections from replica
host    replication     all             db-replica              trust
host    replication     all             172.16.0.0/12           trust
host    replication     all             192.168.0.0/16          trust
